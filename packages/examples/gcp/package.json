{
  "name": "@elizaos/gcp-examples",
  "version": "2.0.0-alpha",
  "description": "elizaOS GCP Cloud Run worker (TypeScript) and test client",
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "test-client": "bun run test-client.ts",
    "test-client:stream": "bun run test-client.ts --stream",
    "build": "esbuild handler.ts --bundle --platform=node --target=node20 --outfile=dist/handler.js --format=esm --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "build:watch": "npm run build -- --watch",
    "dev": "tsx handler.ts",
    "start": "node dist/handler.js",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "docker:build": "docker build -t eliza-worker-ts .",
    "docker:run": "docker run -p 8080:8080 -e OPENAI_API_KEY=$OPENAI_API_KEY eliza-worker-ts",
    "lint": "bunx @biomejs/biome check --write --unsafe .",
    "lint:check": "bunx @biomejs/biome check ."
  },
  "dependencies": {
    "@elizaos/core": "alpha",
    "@elizaos/plugin-openai": "alpha",
    "@elizaos/plugin-sql": "alpha",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@types/uuid": "^11.0.0",
    "esbuild": "^0.28.1",
    "tsx": "^4.19.0",
    "typescript": "^6.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
